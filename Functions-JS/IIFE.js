// Imediately Invoked Function Expressions IIFE.

// this is named IIFE
(function DBCNTONE() {
    console.log("Database one connected");
})();

((name) => { 
    console.log(`Server ${name}, is created `);
})("Google")

